apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubenova-ui
  namespace: kubenova
  labels:
    app: kubenova-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubenova-ui
  template:
    metadata:
      labels:
        app: kubenova-ui
    spec:
      containers:
        - name: ui
          image: ghcr.io/YOUR_ORG/kubenova-ui:latest
          ports:
            - containerPort: 80
          env:
            - name: SERVER_HOST
              value: kubenova-server
            - name: SERVER_PORT
              value: "3001"
          resources:
            requests:
              cpu: 20m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: kubenova-ui
  namespace: kubenova
spec:
  selector:
    app: kubenova-ui
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
